// Loaded by jest.config.cjs `setupFilesAfterEnv`.
// `@testing-library/jest-dom` registers DOM-aware matchers like
// `.toBeInTheDocument()`, `.toHaveAttribute()`, etc. onto Jest's `expect`.
import "@testing-library/jest-dom";
