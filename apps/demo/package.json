{
  "name": "@questkit/demo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "questkit-worker-demo — Vite + React + Tailwind v4 SPA hosted as a static-asset Cloudflare Worker with an inline /api/token JWT-mint proxy.",
  "scripts": {
    "dev": "vite",
    "dev:worker": "vite build && wrangler dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy",
    "deploy:dry-run": "vite build && wrangler deploy --dry-run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "e2e": "playwright test",
    "e2e:prod": "cross-env E2E_TARGET=prod playwright test",
    "e2e:preview": "cross-env E2E_TARGET=prod playwright test",
    "clean": "rimraf dist .turbo .wrangler"
  },
  "dependencies": {
    "@questkit/core": "workspace:^",
    "@questkit/react": "workspace:^",
    "@questkit/types": "workspace:^",
    "framer-motion": "catalog:",
    "hono": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-router-dom": "catalog:"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@tailwindcss/vite": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "cross-env": "^7.0.3",
    "rimraf": "^6.0.1",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "wrangler": "catalog:"
  }
}
